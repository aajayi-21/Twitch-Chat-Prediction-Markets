[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "clob_engine" # This should match your [lib] name
version = "0.1"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "nicegui",
    "twitchapi"
]